[package]
name = "meili"
version = "0.1.0"
edition = "2024"
description = "Agent workflow canvas built on FerrumFlow — custom themed UI."
license = "Apache-2.0"

[dependencies]
ferrum-flow = { workspace = true }
gpui = { workspace = true }
gpui-component = "0.5.1"
serde_json = { workspace = true }
